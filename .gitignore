# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node / yarn
node_modules
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# TypeScript
*.tsbuildinfo

frontend

# Wails build output (二进制 / 应用包；图标和模板要保留)
build/bin/

# Vite 前端打包产物 — 由 wails build 期间的 yarn build 自动生成，不入仓库
# 保留 .gitkeep 让 //go:embed all:frontend-assets 在没跑 yarn build 时也能编译
frontend-assets/*
!frontend-assets/.gitkeep

# Superpowers visual brainstorming scratch files
.superpowers/

# 真实数据 golden snapshot 由本地用户管理，可能包含个人路径/敏感 content_sign
internal/similarity/testdata/golden_real*.json
